apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "HelmAKS.fullname" . }}-test-connection"
  labels:
    {{- include "HelmAKS.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "HelmAKS.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
